This is a simple but functioning calculator, developed with the (eval) function.
eval is a very dangerous function so to help it, i have used (re) function to filter and remove all the unnecessary characters.
